@page "{id:int}"
@model ConvenientStoreManagement.Pages.Inventory.DetailsModel
@{
    ViewData["Title"] = $"Receipt #{Model.Receipt?.ReceiptId} Details";
}

<div class="row mb-4">
    <div class="col-12 d-flex justify-content-between align-items-center">
        <div>
            <h2 class="fw-bold">🔍 Chi tiết phiếu nhập #@Model.Receipt?.ReceiptId</h2>
            <p class="text-muted mb-0">
                Nhân viên: <strong>@(Model.Receipt?.User?.FullName ?? Model.Receipt?.User?.Username ?? $"User #{Model.Receipt?.UserId}")</strong>
                &nbsp;·&nbsp;
                Ngày nhập: <strong>@Model.Receipt?.ImportDate.ToString("dd/MM/yyyy HH:mm")</strong>
            </p>
        </div>
        <a asp-page="/Inventory/History" class="btn btn-outline-secondary">
            ← Quay lại danh sách
        </a>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        @if (!Model.Details.Any())
        {
            <div class="text-center py-5 text-muted">
                <p>Phiếu nhập này không có chi tiết.</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-bordered table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Sản phẩm (Product Name)</th>
                            <th class="text-center">Số lượng (Qty)</th>
                            <th class="text-end">Giá nhập gốc (Original Import Price)</th>
                            <th class="text-end">Giá trung bình (Avg Import Price)</th>
                            <th class="text-end">Thành tiền (Line Total)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var d in Model.Details)
                        {
                            decimal lineTotal = d.Quantity * d.OriginalImportPrice;
                            <tr>
                                <td class="fw-semibold">@(d.Product?.Name ?? $"Product #{d.ProductId}")</td>
                                <td class="text-center">@d.Quantity</td>
                                <td class="text-end">@d.OriginalImportPrice.ToString("N0") VNĐ</td>
                                <td class="text-end text-primary">@d.ImportPrice.ToString("N0") VNĐ</td>
                                <td class="text-end fw-bold text-danger">@lineTotal.ToString("N0") VNĐ</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot class="table-secondary">
                        <tr>
                            <td colspan="4" class="text-end fw-bold">Tổng cộng (Total):</td>
                            <td class="text-end fw-bold text-danger fs-5">
                                @Model.Receipt?.TotalCost.ToString("N0") VNĐ
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="mt-3">
                <p class="text-muted small">
                    <strong>Giá nhập gốc (Original Import Price)</strong>: giá thực tế khi nhập lô hàng này.<br />
                    <strong>Giá trung bình (Avg Import Price)</strong>: giá trung bình có trọng số sau khi gộp với tồn kho cũ.
                </p>
            </div>
        }
    </div>
</div>
