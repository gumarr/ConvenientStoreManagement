@page
@model ConvenientStoreManagement.Pages.Dashboard.IndexModel
@{
    ViewData[index: "Title"] = "Revenue & Profit Statistics";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Revenue & Profit Statistics</h2>
</div>

<!-- ================= SUMMARY ================= -->
<div class="row g-4 mb-4">
    <div class="col-md-3">
        <div class="card bg-primary text-white shadow-sm border-0 position-relative">
            <div class="card-body">
                <h6>Today Revenue</h6>
                <h3>@Model.Stats.TodayRevenue.ToString("N0") ₫</h3>
                @if (!string.IsNullOrEmpty(Model.Stats.TodayStatsSource)) {
                    <span class="position-absolute top-0 end-0 badge bg-light text-dark m-2">Source: @Model.Stats.TodayStatsSource</span>
                }
                @if (!string.IsNullOrEmpty(Model.Stats.TodayStatsCreatedBy)) {
                    <small class="d-block mt-2 opacity-75">By: @Model.Stats.TodayStatsCreatedBy</small>
                }
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card bg-success text-white shadow-sm border-0">
            <div class="card-body">
                <h6>Today Profit</h6>
                <h3>@Model.Stats.TodayProfit.ToString("N0") ₫</h3>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card bg-info text-white shadow-sm border-0">
            <div class="card-body">
                <h6>This Week Revenue</h6>
                <h3>@Model.Stats.ThisWeekRevenue.ToString("N0") ₫</h3>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card bg-warning shadow-sm border-0">
            <div class="card-body">
                <h6>This Month Revenue</h6>
                <h3>@Model.Stats.ThisMonthRevenue.ToString("N0") ₫</h3>
            </div>
        </div>
    </div>
</div>

<!-- ================= CHART + TOP ================= -->
<div class="row g-4 mb-4">
    <div class="col-lg-8">
        <div class="card shadow-sm border-0">
            <div class="card-header">Revenue & Profit (30 days)</div>
            <div class="card-body">
                <canvas id="revenueChart"></canvas>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card shadow-sm border-0">
            <div class="card-header">Top 5 Products</div>
            <div class="card-body">
                @foreach (ViewModels.TopProductDto p in Model.Stats.TopProducts)
                {
                    <div class="d-flex justify-content-between">
                        <span>@p.ProductName</span>
                        <b>@p.TotalQuantitySold</b>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<!-- ================= LOW STOCK ================= -->
@if (Model.Stats.LowStockProductCount > 0)
{
    <div class="card border-danger border-2 mb-4">
        <div class="card-header text-danger">⚠️ Low Stock</div>
        <div class="card-body">
            @foreach (ViewModels.LowStockProductDto p in Model.Stats.LowStockProducts)
            {
                <div class="d-flex justify-content-between">
                    <span>@p.ProductName</span>
                    <b class="text-danger">@p.CurrentStock</b>
                </div>
            }
        </div>
    </div>
}

<!-- ================= AI ================= -->
<form method="post" asp-page-handler="AISuggest">
    <button class="btn btn-dark mb-3">🤖 AI Suggest</button>
</form>

@if (Model.AIResult != null && !string.IsNullOrEmpty(Model.AIResult.Content))
{
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <span>AI Suggestion</span>
            @if (Model.AIResult.User != null)
            {
                <small class="badge bg-secondary">
                    Requested by: @Model.AIResult.User.FullName - @Model.AIResult.CreatedAt.ToString("dd/MM/yyyy HH:mm")
                </small>
            }
        </div>
        <div class="card-body">
            @Html.Raw(Model.AIResult.Content.Replace("\n", "<br/>"))
        </div>
    </div>
}

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        const labels = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Stats.ChartLabels));
        const revenue = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Stats.ChartRevenue));
        const profit = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Stats.ChartProfit));

        const ctx = document.getElementById('revenueChart');

        new Chart(ctx, {
        type: 'line',
        data: {
        labels: labels,
        datasets: [
        {
        label: 'Revenue',
        data: revenue,
        borderColor: '#0d6efd'
        },
        {
        label: 'Profit',
        data: profit,
        borderColor: '#198754'
        }
        ]
        }
        });
    </script>
}
